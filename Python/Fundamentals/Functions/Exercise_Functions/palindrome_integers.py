def is_palindrome(number):
    for char in list_of_strings:
        is_palindrome = False
        if char[0] == char[-1]:
            is_palindrome = True
            print(is_palindrome)
        else:
            print(is_palindrome)

list_of_strings = input().split(', ')
is_palindrome(list_of_strings)