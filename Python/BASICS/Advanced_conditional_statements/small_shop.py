product = input()
city = input()
quantity = float(input())

if city == "Sofia":
    if product == "coffee":
        price_coffee = 0.50
        cost = quantity * price_coffee
        print(cost)
    if product == "water":
        price_water = 0.80
        cost = quantity * price_water
        print(cost)
    if product == "beer":
        price_beer = 1.20
        cost = quantity * price_beer
        print(cost)
    if product == "sweets":
        price_sweets = 1.45
        cost = quantity * price_sweets
        print(cost)
    if product == "peanutes":
        price_peanuts = 1.60
        cost = quantity * price_peanuts
        print(cost)

if city == "Plovdiv":
    if product == "coffee":
        price_coffee = 0.40
        cost = quantity * price_coffee
        print(cost)
    if product == "water":
        price_water = 0.70
        cost = quantity * price_water
        print(cost)
    if product == "beer":
        price_beer = 1.15
        cost = quantity * price_beer
        print(cost)
    if product == "sweets":
        price_sweets = 1.30
        cost = quantity * price_sweets
        print(cost)
    if product == "peanuts":
        price_peanuts = 1.50
        cost = quantity * price_peanuts
        print(cost)

if city == "Varna":
    if product == "coffee":
        price_coffee = 0.45
        cost = quantity * price_coffee
        print(cost)
    if product == "water":
        price_water = 0.70
        cost = quantity * price_water
        print(cost)
    if product == "beer":
        price_beer = 1.10
        cost = quantity * price_beer
        print(cost)
    if product == "sweets":
        price_sweets = 1.35
        cost = quantity * price_sweets
        print(cost)
    if product == "peanutes":
        price_peanuts = 1.55
        cost = quantity * price_peanuts
        print(cost)